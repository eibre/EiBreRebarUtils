<Window x:Class="EiBreRebarUtils.WindowTextInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EiBreRebarUtils"
            SizeToContent="WidthAndHeight"
        Title="EiBre Rebar Utils: Text Input"
        WindowStartupLocation="CenterScreen"
        Topmost="True">
    <Grid Margin="15" MinWidth="250" FocusManager.FocusedElement="{Binding ElementName=textInput}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label>Write text:</Label>
        <TextBox Grid.Row="1" Name="textInput"></TextBox>
        <WrapPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button MinWidth="60" IsDefault="True" Click="Button_Click">OK</Button>
            <Button Margin="15,0,0,0" MinWidth="60" IsCancel="True">Cancel</Button>
        </WrapPanel>
    </Grid>
</Window>
